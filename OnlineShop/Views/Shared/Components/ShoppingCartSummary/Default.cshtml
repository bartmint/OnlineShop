@model int
@if (Model > 0)
{
    <a asp-controller="Cart" asp-action="Index">
        <i class="fas fa-shopping-cart"></i>
        <span id="cart-status">
            @Model
        </span>
    </a>
}
